class Configuration {

}

// configuration = new Configuration()
// configuration.add_piece(pawn, row=2, col=3)
// configuration.move_piece(pawn, from, to)
//
// [(piece1, row1, col1), (piece2, row2, col2).....] = configuration.get_all_pieces()

setup_initial_configuration() {
    configuration = new Configuration()

    // Add white pawns:
    
}